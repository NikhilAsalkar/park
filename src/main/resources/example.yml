server:
  applicationConnectors:
    - type: http
      port: 9000
  adminConnectors:
    - type: http
      port: 9001

logging:
  appenders:
    - type: file
      currentLogFilename: ./parklog.log
      archivedLogFilenamePattern: ./parklog-%d.log.gz
